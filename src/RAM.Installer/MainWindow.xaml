<Window x:Class="RAM.Installer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Roblox Account Manager — Installer"
        Height="520" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BgBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        FontSize="13"
        UseLayoutRounding="True">

    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="28,10" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Padding" Value="22,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource PanelHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModeCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="Tag" Value="Selected">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="2" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernProgress" TargetType="ProgressBar">
            <Setter Property="Height" Value="6" />
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border x:Name="PART_Track" Background="{TemplateBinding Background}" CornerRadius="3" />
                            <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" CornerRadius="3" HorizontalAlignment="Left" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="32,26,32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Roblox Account Manager" FontSize="26" FontWeight="SemiBold" />
        <TextBlock x:Name="VersionText" Grid.Row="1" Margin="0,2,0,18" FontSize="13" Foreground="{StaticResource MutedBrush}" />

        <!-- Choose screen -->
        <StackPanel x:Name="ChoosePanel" Grid.Row="2">
            <Border x:Name="MachineCard" Style="{StaticResource ModeCard}" Tag="Selected" MouseLeftButtonUp="MachineCard_Click">
                <StackPanel>
                    <TextBlock FontSize="15" FontWeight="SemiBold">Install for all users</TextBlock>
                    <TextBlock Margin="0,3,0,0" FontSize="12" Foreground="{StaticResource MutedBrush}"
                               TextWrapping="Wrap">Installs to Program Files with a Start Menu shortcut and an entry in Settings → Apps, so the app can be uninstalled later.</TextBlock>
                </StackPanel>
            </Border>

            <Border x:Name="PortableCard" Style="{StaticResource ModeCard}" Margin="0,10,0,0" MouseLeftButtonUp="PortableCard_Click">
                <StackPanel>
                    <TextBlock FontSize="15" FontWeight="SemiBold">Portable (no installation)</TextBlock>
                    <TextBlock Margin="0,3,0,0" FontSize="12" Foreground="{StaticResource MutedBrush}"
                               TextWrapping="Wrap">Extracts to a folder you choose — no registry, no shortcuts, nothing system-wide. Run the exe straight from the folder.</TextBlock>
                </StackPanel>
            </Border>

            <Grid Margin="0,18,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Install to" Foreground="{StaticResource MutedBrush}" />
                <TextBox x:Name="PathBox" Grid.Column="1" Margin="14,0" Height="34" VerticalContentAlignment="Center"
                         Background="{StaticResource PanelBrush}" Foreground="{StaticResource TextBrush}"
                         BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="10,0" />
                <Button x:Name="BrowseButton" Grid.Column="2" Content="Browse…" Style="{StaticResource SecondaryButton}" Click="Browse_Click" />
            </Grid>

            <TextBlock Margin="0,14,0,0" FontSize="11.5" Foreground="{StaticResource MutedBrush}"
                       Text="No internet connection required — everything is bundled in this file." />
        </StackPanel>

        <!-- Working screen -->
        <StackPanel x:Name="WorkingPanel" Grid.Row="2" Visibility="Collapsed" VerticalAlignment="Center">
            <TextBlock x:Name="WorkingTitle" FontSize="18" FontWeight="SemiBold" Text="Installing…" />
            <ProgressBar x:Name="Progress" Style="{StaticResource ModernProgress}" Margin="0,18,0,10" />
            <TextBlock x:Name="ProgressText" FontSize="12" Foreground="{StaticResource MutedBrush}" />
        </StackPanel>

        <!-- Done screen -->
        <StackPanel x:Name="DonePanel" Grid.Row="2" Visibility="Collapsed" VerticalAlignment="Center">
            <TextBlock x:Name="DoneIcon" FontFamily="Segoe Fluent Icons" FontSize="34" Foreground="{StaticResource AccentBrush}" Text="&#xE73E;" />
            <TextBlock x:Name="DoneTitle" Margin="0,10,0,0" FontSize="18" FontWeight="SemiBold" />
            <TextBlock x:Name="DoneDetail" Margin="0,4,0,0" FontSize="12.5" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
        </StackPanel>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="0,22,0,0" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0"
                       FontSize="12.5" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="SecondaryButton" Content="Cancel" Style="{StaticResource SecondaryButton}" Click="Secondary_Click" />
                <Button x:Name="PrimaryButton" Content="Install" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" Click="Primary_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
