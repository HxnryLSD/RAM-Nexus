namespace RAM.Core.Roblox.FastFlags;

/// <summary>The value kind of a fast flag (used to pick the right editor control + serialization).</summary>
public enum FastFlagType
{
    Boolean,
    Integer
}
